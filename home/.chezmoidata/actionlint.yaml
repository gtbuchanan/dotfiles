# Version for the Termux out-of-band actionlint install, by
# .chezmoiscripts/android/run_onchange_after_install-actionlint.sh.tmpl.
# actionlint is a standalone GitHub Actions linter that gtbuchanan/tooling's
# hk pipeline orchestrates as one step; kept in sync with tooling's mise.toml.
#
---
# renovate: datasource=github-releases depName=rhysd/actionlint
actionlint_version: '1.7.12'
